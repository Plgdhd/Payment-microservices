spring:
  config:
    activate:
      on-profile: docker
  data:
    mongodb:
      host: ${MONGO_HOST:payment-mongo}
      port: ${MONGO_PORT:27017}
      database: ${MONGO_DB_NAME:payments}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:payment-kafka:9092}
payment:
  externalApiUrl: ${PAYMENT_EXTERNAL_API_URL:http://external-api-service/api/number}
